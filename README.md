# React + Vite

Once forked/cloned the repo, make sure to run following commands in order to ensure all dependencies are installed for the app:

1)install all dependencies for both client and server using the following commands within the directories: npm i

2)In order to run both client and server use the following commands:
-npm run dev: for dev mode changes
-npm
